{% extends 'base.html.twig' %}

{% block title %}Material Details {% endblock %}

 {% block pageheader %} 
    <h1>Material Details</h1>
 {% endblock %} 

{% block body %}

    {% if is_granted('ROLE_ADMIN') %}
    <div class='btn-toolbar pull-right'>
        <div class='btn-group'>
            <a href="{{ path('material_edit', {'id': material.id }) }}" class="btn btn-default">
                <span class="glyphicon glyphicon-edit"></span> Edit
            </a>
        </div>
        <div class='btn-group'>
            <form class='delete-form' method="post" action="{{ path('material_delete', {'id': material.id }) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                <input type="hidden" name="_method" value="DELETE">
                <input type="hidden" name="_token" value="{{ csrf_token("delete" ~ material.id) }}">
                <button class="btn btn-danger"><span class="glyphicon glyphicon-trash"></span> Delete</button>
            </form>
        </div>
    </div>
    {% endif %}

    <table class="table table-bordered table-condensed table-hover table-striped">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ material.name }}</td>
            </tr>
        <tr>
                <th>Label</th>
                <td>{{ material.label }}</td>
            </tr>
        <tr>
                <th>Description</th>
                <td>{{ material.description }}</td>
            </tr>
                <tr>
            <th>Created</th>
            <td>{{ material.created|date }}</td>
        </tr>
        <tr>
            <th>Updated</th>
            <td>{{ material.updated|date }}</td>
        </tr>
        </tbody>
    </table>

{% endblock %}
