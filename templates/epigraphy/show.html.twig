{% extends 'base.html.twig' %}

{% block title %}Epigraphy Details {% endblock %}

 {% block pageheader %} 
    <h1>Epigraphy Details</h1>
 {% endblock %} 

{% block body %}

    {% if is_granted('ROLE_ADMIN') %}
    <div class='btn-toolbar pull-right'>
        <div class='btn-group'>
            <a href="{{ path('epigraphy_edit', {'id': epigraphy.id }) }}" class="btn btn-default">
                <span class="glyphicon glyphicon-edit"></span> Edit
            </a>
        </div>
        <div class='btn-group'>
            <form class='delete-form' method="post" action="{{ path('epigraphy_delete', {'id': epigraphy.id }) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                <input type="hidden" name="_method" value="DELETE">
                <input type="hidden" name="_token" value="{{ csrf_token("delete" ~ epigraphy.id) }}">
                <button class="btn btn-danger"><span class="glyphicon glyphicon-trash"></span> Delete</button>
            </form>
        </div>
    </div>
    {% endif %}

    <table class="table table-bordered table-condensed table-hover table-striped">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ epigraphy.name }}</td>
            </tr>
        <tr>
                <th>Label</th>
                <td>{{ epigraphy.label }}</td>
            </tr>
        <tr>
                <th>Description</th>
                <td>{{ epigraphy.description }}</td>
            </tr>
                <tr>
            <th>Created</th>
            <td>{{ epigraphy.created|date }}</td>
        </tr>
        <tr>
            <th>Updated</th>
            <td>{{ epigraphy.updated|date }}</td>
        </tr>
        </tbody>
    </table>

{% endblock %}
