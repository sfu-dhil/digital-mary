{%- set authors = contributions|filter(c => 'aut' in c.roleValues)|map(c => c.person) -%}
{{ authors|map(a => "#{a.citationName}.")|sort|join(', ') }} &ldquo;{{ title }}.&rdquo; The Digital Mary Project. Simon Fraser
University, {{ updated|date('Y') }}. {{ app.request.uri }}.
