<table class='table table-bordered table-condensed table-hover table-striped'>
    <tbody>
        <tr>
            <th>Name</th>
            <td>
                {{ inscription_style.name }}
            </td>
        </tr>
        <tr>
            <th>Label</th>
            <td>
                {{ inscription_style.label }}
            </td>
        </tr>
        <tr>
            <th>Description</th>
            <td>
                {{ inscription_style.description|raw }}
            </td>
        </tr>

        <tr>
            <th>Item</th>
            <td>
                {% if inscription_style.items|length > 0 %}
                    <ul>
                        {% for item in inscription_style.items %}
                            <li>
                                <a href='{{ path("item_show", {"id":item.id }) }}'>
                                    {{ item }}
                                </a>
                            </li>
                        {% endfor %}
                    </ul>
                {% endif %}
            </td>
        </tr>
        <tr>
            <th>Created</th>
            <td>{{ inscription_style.created|date }}</td>
        </tr>
        <tr>
            <th>Updated</th>
            <td>{{ inscription_style.updated|date }}</td>
        </tr>
    </tbody>
</table>


