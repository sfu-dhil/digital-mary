<table class='table table-bordered table-condensed table-hover table-striped'>
    <tbody>
        <tr>
            <th>Name</th>
            <td>
                {{ language.name }}
            </td>
        </tr>
        <tr>
            <th>Label</th>
            <td>
                {{ language.label }}
            </td>
        </tr>
        <tr>
            <th>Description</th>
            <td>
                {{ language.description|raw }}
            </td>
        </tr>

        <tr>
            <th>Item</th>
            <td>
                {% if language.items|length > 0 %}
                    <ul>
                        {% for item in language.items %}
                            <li>
                                <a href='{{ path("item_show", {"id":item.id }) }}'>
                                    {{ item }}
                                </a>
                            </li>
                        {% endfor %}
                    </ul>
                {% endif %}
            </td>
        </tr>
        <tr>
            <th>Created</th>
            <td>{{ language.created|date }}</td>
        </tr>
        <tr>
            <th>Updated</th>
            <td>{{ language.updated|date }}</td>
        </tr>
    </tbody>
</table>


