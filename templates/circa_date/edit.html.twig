    {% extends 'base.html.twig' %}

    {% block title %}Edit Circa Date {% endblock %}

    {% block body %}
    <h1>Edit Circa Date</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            <input type="submit" value="Save" class="btn btn-primary" />
            <a href="{{ path('circa_date_show', { 'id': circa_date.id }) }}" class="btn btn-warning">
                Cancel
            </a>
        </div>
    </div>
    {{ form_end(form) }}

    {% endblock %}

    {% block javascripts %}
    {% include '@NinesEditor/editor/widget.html.twig' %}
    {% endblock %}
