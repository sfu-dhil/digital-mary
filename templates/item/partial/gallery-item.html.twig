

{% if item is defined and item.name is defined %}
    <div class="item">
        <a class="item-card" href="{{ path('item_show', {'id': item.id}) }}">
            <div class="item-img-wrapper">
                <div>
                    {% include 'partial/image.html.twig' with {'image': item.images[0] ?? null}%}
                </div>
            </div>
            {% if item is defined and item.name is defined %}
                <div class="item-content-wrapper">
                    <div class="item-content">
                        <h2 class="item-content-header">{{ item.name }}</h2>
                        <div class="item-content-desc">
                            {{ item.displayYear }}
                        </div>
                    </div>
                </div>
            {% endif %}
        </a>
    </div>
{% endif %}

