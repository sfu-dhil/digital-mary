{% if items|length > 0 %}
    <div class="gallery">
        {%  for item in items %}
            {% include 'item/partial/gallery-item.html.twig' %}
        {% endfor %}
        {% block callback %}{% endblock %}
    </div>
{% endif %}
